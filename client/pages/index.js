import { BaseColors } from "../sections";

const Page = () => {
  //const currentTheme = theme === "system" ? currentTheme : theme;

  return (
    <div className="overflow-hidden">
      <BaseColors className="z-10" />
    </div>
  );
};

export default Page;
